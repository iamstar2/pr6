# Windows용 Mosquitto 설정 (Docker/WSL 사용 안 함, 개발/테스트용 anonymous 허용)
# 실행: mosquitto -c mosquitto.conf -v   (Mosquitto 설치 폴더에 PATH가 잡혀 있어야 함)

listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true
